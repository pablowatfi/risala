[pytest]
asyncio_mode = auto
testpaths = tests
addopts = -v --tb=short
markers =
    unit: fast tests with no LLM or external dependencies
    integration: requires a running LLM provider (set LLM_PROVIDER in .env)
    gmail: requires Gmail OAuth token files in credentials/
